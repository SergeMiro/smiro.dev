---
import PcEvolution from './PcEvolution.astro';
---

<section class="relative min-h-screen flex items-center pt-16 overflow-hidden">
  <!-- Abstract mesh gradient background -->
  <div class="absolute inset-0 opacity-30" aria-hidden="true">
    <div class="absolute top-1/4 -left-1/4 w-[600px] h-[600px] rounded-full bg-[var(--color-primary)] opacity-[0.07] blur-[120px] animate-pulse-slow"></div>
    <div class="absolute bottom-1/4 -right-1/4 w-[500px] h-[500px] rounded-full bg-[var(--color-accent)] opacity-[0.05] blur-[100px] animate-pulse-slow" style="animation-delay: 2s"></div>
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[400px] h-[400px] rounded-full bg-[oklch(0.7_0.15_140)] opacity-[0.03] blur-[90px] animate-pulse-slow" style="animation-delay: 4s"></div>
  </div>

  <div class="relative mx-auto max-w-7xl px-6 py-16 md:py-24 w-full">
    <!-- Top row: Badge -->
    <div class="flex justify-between items-start mb-6 flex-col-reverse sm:flex-row gap-4">
      <div data-reveal></div>
      <div class="badge-3d self-start sm:self-auto" data-reveal>
        <div class="badge-3d-inner">
          <span class="w-2 h-2 rounded-full bg-emerald-400 animate-pulse flex-shrink-0"></span>
          Available for freelance
        </div>
      </div>
    </div>

    <!-- Two-column layout: text + animation -->
    <div class="grid lg:grid-cols-2 gap-8 lg:gap-12 items-center">
      <!-- Left: Text content -->
      <div>
        <h1
          data-split="chars"
          class="font-[var(--font-display)] text-[length:clamp(1.8rem,4.5vw,4.5rem)] font-bold leading-[0.95] tracking-tight"
        >
          Full Stack Developer.<br />
          <span class="text-[var(--color-primary)]">AI Whisperer.</span><br />
          Automation Addict.
        </h1>

        <p class="hero-sub mt-6 max-w-xl text-lg md:text-xl text-[var(--color-text-muted)] leading-relaxed">
          I build web apps, teach LLMs to work for me, and automate everything else
          — based in Dijon, France.
        </p>

        <div class="hero-ctas mt-8 flex flex-wrap gap-4">
          <a
            href="/projects"
            class="code-trigger inline-flex items-center gap-2 px-6 py-3 bg-[var(--color-primary)] text-[var(--color-bg)] font-medium rounded-lg hover:bg-[var(--color-primary-hover)] transition-all duration-300 hover:shadow-lg hover:shadow-[var(--color-primary)]/20 active:scale-[0.97]"
            data-prefetch
          >
            View Projects
            <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M5 12h14M12 5l7 7-7 7" />
            </svg>
          </a>
          <a
            href="/contact"
            class="code-trigger inline-flex items-center gap-2 px-6 py-3 border border-[var(--color-border)] text-[var(--color-text)] font-medium rounded-lg hover:border-[var(--color-primary)] hover:text-[var(--color-primary)] transition-all duration-300 active:scale-[0.97]"
            data-prefetch
          >
            Contact Me
          </a>
        </div>
      </div>

      <!-- Right: PC Evolution Animation -->
      <div class="order-first lg:order-last" data-reveal>
        <PcEvolution />
      </div>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2 flex flex-col items-center gap-2" data-reveal>
    <span class="text-xs text-[var(--color-text-faint)] tracking-widest uppercase">Scroll</span>
    <div class="w-px h-12 bg-gradient-to-b from-[var(--color-text-faint)] to-transparent animate-scroll-line"></div>
  </div>
</section>

<style>
  @keyframes pulse-slow {
    0%, 100% { opacity: 0.07; transform: scale(1); }
    50% { opacity: 0.12; transform: scale(1.05); }
  }
  .animate-pulse-slow {
    animation: pulse-slow 8s ease-in-out infinite;
  }
  @keyframes scroll-line {
    0% { transform: scaleY(0); transform-origin: top; }
    50% { transform: scaleY(1); transform-origin: top; }
    51% { transform: scaleY(1); transform-origin: bottom; }
    100% { transform: scaleY(0); transform-origin: bottom; }
  }
  .animate-scroll-line {
    animation: scroll-line 2s ease-in-out infinite;
  }
</style>
